/**
 * We re going to create our own custom  history object and use it
 * to implement some programmatic navigation in our app.
 */


import { createBrowserHistory } from 'history'; //history library installed with react-router-dom


/* export object returned by function call */
export default createBrowserHistory();